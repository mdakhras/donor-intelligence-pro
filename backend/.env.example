# -----------------------------------------------------------------------------
# Environment variables for the Donor Intelligence Assistant
#
# Duplicate this file as .env and replace the placeholder values with your
# actual credentials and configuration.
# -----------------------------------------------------------------------------

# --- Azure OpenAI Settings (for CrewAI) ---
# Used by the backend to run the analysis agents.
OPENAI_API_BASE="https://your-aoai-resource.openai.azure.com"
OPENAIAPI_KEY="your_azure_openai_api_key"
OPENAI_API_VERSION="2024-05-01-preview"
OPENAI_MODEL="your_azure_openai_deployment_name" # This is the DEPLOYMENT name, not the model name

# --- Azure Storage Settings (for PDF uploads) ---
# Used by the Streamlit frontend to upload files.
AZURE_STORAGE_CONNECTION_STRING="your_storage_account_connection_string"
AZURE_STORAGE_CONTAINER_NAME="pdf-uploads"

# --- Azure Cosmos DB Settings (for request queuing) ---
# Used by both the frontend (to write) and the backend (to read/update).
AZURE_COSMOS_CONNECTION_STRING="your_cosmosdb_connection_string"
AZURE_COSMOS_DATABASE_NAME="DonorIntelDB"
AZURE_COSMOS_CONTAINER_NAME="Requests"
